[project]
name = "terraform-deploy-guide"
version = "0.1.0"
description = "A guide to deploy django projects in the Cloud using Terraform."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
